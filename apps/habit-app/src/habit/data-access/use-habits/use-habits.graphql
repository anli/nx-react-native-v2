subscription Habits($minDate: DateTime!, $maxDate: DateTime!) {
  queryHabit {
    habitActivities(
      filter: { date: { between: { min: $minDate, max: $maxDate } } }
    ) {
      __typename
      id
      count
      date
    }
    __typename
    id
    name
  }
}
