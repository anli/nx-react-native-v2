subscription Group($id: ID!) {
  getGroup(id: $id) {
    id
    name
    adminUsersAggregate {
      count
    }
  }
}

query GroupNonSubscription($id: ID!) {
  getGroup(id: $id) {
    id
    name
    adminUsers {
      email
    }
  }
}
